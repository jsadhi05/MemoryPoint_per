import React from 'react';
import { Text, Button, View } from 'react-native';

export const Register = ({ navigation }) => {
    return (
        <View>
            <Text>This is Register component </Text>
        </View>
    )
}